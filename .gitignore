# Provided assignment data -- never commit, reference by path (see README).
/data/
*.db
*.sqlite

# Derived/build artifacts -- regenerated by the ETL step, not source.
/cache/
/warehouse/*.duckdb
/warehouse/*.duckdb.wal

# Python
__pycache__/
*.pyc
.venv/
.env

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/vite.config.js.timestamp-*.mjs

# OS
.DS_Store

# Cowork-generated project tracker / sandbox debris, not part of the
# submission repo.
Kestrel_Control_Tower_Project_Tracker.xlsx
throwaway.txt

# Claude Code local session settings -- machine/session-specific, not
# project source. Never committed by any earlier commit in this repo.
.claude/

# Unreferenced diagram-export PNGs -- no .md file in this repo links to
# docs/diagrams/ (docs/HLD.md's actual diagrams are the 9 inline mermaid
# blocks already in that file, which render natively on GitHub); these are
# a later, untracked, orphaned export with no commit history behind them.
# Not deleted -- if you want them as real doc assets, link them from
# HLD.md and un-ignore; left out of the repo as-is to avoid committing
# ~4MB of unreferenced binary images.
docs/diagrams/
